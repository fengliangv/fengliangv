#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<string.h>
MAX(int x,int y)
{
	if (x > y)
		return x;
	else
		return y;
}
int main()
{
	int a, b;
	int max = 0;
	scanf("%d%d", &a, &b);
	max=MAX(a, b);
	printf("%d\n", max);
	return 0;
}
